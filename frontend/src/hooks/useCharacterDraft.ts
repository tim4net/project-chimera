/**
 * @fileoverview Re-export of useCharacterDraft hook from context.
 * Provides a convenient import path for components using the character draft.
 */

export { useCharacterDraft } from '../context/characterDraft';
